# ── AI-assisted files (not for repo) ──────────────
# Root-anchored: only the top-level docs/ folder. A bare "docs/" would also
# ignore nested dirs like src/app/api/kb/docs/ (which broke the wiki in prod).
/docs/
mockups/
Orbital.md
ad-video.html
.claude/
FEATURES.md
Novara PRD v1.0.md

# ── Database (schema/migrations stay local) ───────
supabase/
src/db/*.sql

# ── Next.js ───────────────────────────────────────
.next/
out/
build/

# ── Dependencies ──────────────────────────────────
node_modules/
.npm

# ── Environment (never commit secrets) ────────────
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# ── OS ────────────────────────────────────────────
.DS_Store
Thumbs.db

# ── Editor ────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo

# ── Testing ───────────────────────────────────────
coverage/

# ── Misc ──────────────────────────────────────────
*.log
.vercel
*.Rhistory
.Rhistory
tsconfig.tsbuildinfo
next-env.d.ts
.env.local.save
